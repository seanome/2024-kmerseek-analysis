# Nextflow run state and per-run reports: regenerated on every launch, and the
# trace/timeline/report names carry the run date so they pile up fast. Same rules
# as the sibling qfo-pfam-region-benchmark pipeline.
.nextflow/
.nextflow.log*
work/
results/
results-test/
*.report.html
*.timeline.html
*.trace.txt
